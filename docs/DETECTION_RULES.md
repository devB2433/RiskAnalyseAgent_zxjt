# 安全分析检测规则文档

## 概述

安全分析系统现已支持**8种完整的检测规则**，覆盖主流网络安全威胁场景。

## 🎯 支持的检测类型

### 1. 失陷主机检测 (Compromised Host Detection)

**检测目标**: 识别已被攻击者控制的主机

**关键指标**:
- 异常网络连接（连接已知C2服务器）
- 异常进程启动
- 异常文件访问
- 异常DNS查询

**分析流程**:
1. 初步日志分析 → 识别异常行为
2. 提取可疑IOC → IP、域名、文件哈希
3. 工具验证 → IOC查询、黑名单检查
4. 生成报告 → 失陷主机列表、证据链、建议措施

**使用场景**:
- APT攻击检测
- 僵尸网络识别
- 后门程序发现

---

### 2. 异常登录检测 (Anomalous Login Detection)

**检测目标**: 识别异常的登录行为

**关键指标**:
- 异常时间登录（非工作时间）
- 异常地点登录（地理位置异常）
- 异常设备登录（新设备）
- 异常登录频率（暴力破解）

**分析流程**:
1. 分析登录行为 → 识别异常模式
2. 建立基线 → 正常登录时间、地点、设备
3. 识别偏离 → 标记异常登录
4. 生成报告 → 异常登录列表、风险评分、建议措施

**使用场景**:
- 账号被盗检测
- 暴力破解识别
- 内部威胁预警

---

### 3. 数据外泄检测 (Data Exfiltration Detection) 🆕

**检测目标**: 识别敏感数据被窃取的行为

**关键指标**:
- 异常大量数据传输（上传）
- 向外部/未知目标传输数据
- 非工作时间的大量数据传输
- 使用加密隧道或代理传输
- 访问敏感数据后立即外传

**分析流程**:
1. 分析网络流量 → 识别大量上传
2. 提取传输记录 → 源、目标、数据量、时间
3. 验证目标 → IOC查询外部IP
4. 生成报告 → 外泄事件、数据量、风险评分

**使用场景**:
- 数据泄露事件调查
- 内部人员监控
- 合规审计

**示例日志**:
```python
SecurityLog(
    log_type="firewall",
    source_ip="192.168.1.50",
    dest_ip="203.0.113.100",  # 外部IP
    dest_port=443,
    protocol="HTTPS",
    action="allow",
    raw_data={"bytes": 1073741824}  # 1GB上传
)
```

---

### 4. 恶意软件检测 (Malware Detection) 🆕

**检测目标**: 识别恶意软件活动

**关键指标**:
- 可疑进程启动（未知程序、异常路径）
- 文件哈希匹配已知恶意软件
- 异常系统行为（注册表修改、服务创建）
- 网络通信特征（C2连接、DGA域名）
- 持久化机制（启动项、计划任务）

**分析流程**:
1. 分析EDR日志 → 识别可疑进程和文件
2. 提取特征 → 进程、文件哈希、网络连接
3. 哈希验证 → 查询威胁情报库
4. 生成报告 → 恶意软件列表、类型、感染主机

**使用场景**:
- 病毒木马检测
- 勒索软件识别
- 挖矿程序发现

**示例日志**:
```python
SecurityLog(
    log_type="edr",
    source_ip="192.168.1.50",
    action="process_start",
    raw_data={
        "process": "suspicious.exe",
        "path": "C:\\Users\\Public\\suspicious.exe",
        "hash": "abc123def456789",
        "parent_process": "explorer.exe"
    }
)
```

---

### 5. 内部威胁检测 (Insider Threat Detection) 🆕

**检测目标**: 识别内部人员的恶意行为

**关键指标**:
- 异常数据访问（访问敏感数据、越权访问）
- 数据下载/复制行为（大量下载、使用U盘）
- 异常工作时间（非工作时间活动）
- 权限提升尝试
- 离职前异常行为

**分析流程**:
1. 分析用户行为 → 识别异常操作
2. 建立基线 → 正常工作模式
3. 识别偏离 → 标记高风险用户
4. 生成报告 → 风险用户、威胁类型、证据链

**使用场景**:
- 员工监控
- 离职风险管理
- 数据泄露预防

**示例日志**:
```python
SecurityLog(
    log_type="file_access",
    source_ip="192.168.1.50",
    action="copy",
    raw_data={
        "username": "john.doe",
        "file": "//fileserver/confidential/customer_database.db",
        "destination": "E:\\",  # U盘
        "size": 524288000  # 500MB
    }
)
```

---

### 6. DDoS攻击检测 (DDoS Detection) 🆕

**检测目标**: 识别分布式拒绝服务攻击

**关键指标**:
- 流量异常激增（请求数、带宽）
- 来源IP分布（大量不同IP、地理分布）
- 请求特征（相似请求、异常User-Agent）
- 攻击类型（SYN Flood、HTTP Flood、DNS放大）
- 目标服务和端口

**分析流程**:
1. 分析流量 → 识别异常激增
2. 统计特征 → IP数量、地理分布、请求模式
3. 识别类型 → 判断攻击类型
4. 生成报告 → 攻击规模、来源、受影响服务

**使用场景**:
- 网站防护
- 服务可用性监控
- 攻击溯源

**示例日志**:
```python
# 大量SYN请求
for i in range(100):
    SecurityLog(
        log_type="firewall",
        source_ip=f"10.0.{i//256}.{i%256}",  # 不同源IP
        dest_ip="192.168.1.100",
        dest_port=80,
        protocol="TCP",
        action="syn"
    )
```

---

### 7. 横向移动检测 (Lateral Movement Detection) 🆕

**检测目标**: 识别攻击者在内网的横向移动

**关键指标**:
- 内网扫描行为（端口扫描、主机发现）
- 横向认证尝试（多主机登录、Pass-the-Hash）
- 远程执行（PSExec、WMI、RDP）
- 文件共享访问（SMB、NFS）
- 凭证窃取和使用

**分析流程**:
1. 分析网络活动 → 识别扫描和横向连接
2. 构建攻击路径 → 主机跳转序列
3. 识别技术 → 使用的工具和方法
4. 生成报告 → 攻击路径图、失陷主机、建议措施

**使用场景**:
- APT攻击追踪
- 内网渗透检测
- 攻击链分析

**示例日志**:
```python
# 扫描
SecurityLog(
    log_type="firewall",
    source_ip="192.168.1.50",  # 初始失陷主机
    dest_ip="192.168.1.51",
    dest_port=445,
    action="scan"
)

# 横向认证
SecurityLog(
    log_type="auth",
    source_ip="192.168.1.50",
    dest_ip="192.168.1.51",
    protocol="SMB",
    action="success",
    raw_data={"method": "NTLM"}
)
```

---

### 8. 钓鱼攻击检测 (Phishing Detection) 🆕

**检测目标**: 识别钓鱼邮件和网站

**关键指标**:
- 可疑邮件（伪造发件人、钓鱼链接、恶意附件）
- 钓鱼网站访问（仿冒域名、SSL证书异常）
- 凭证输入行为（在可疑网站输入密码）
- 点击率分析（大量用户点击同一链接）
- 攻击时间和目标

**分析流程**:
1. 分析邮件和Web日志 → 识别钓鱼特征
2. 提取特征 → 邮件特征、钓鱼网站
3. 域名验证 → 查询域名信誉
4. 生成报告 → 钓鱼活动、受影响用户、建议措施

**使用场景**:
- 邮件安全
- 用户安全培训
- 钓鱼活动追踪

**示例日志**:
```python
# 钓鱼邮件
SecurityLog(
    log_type="email",
    source_ip="203.0.113.50",
    action="received",
    raw_data={
        "from": "admin@micros0ft.com",  # 仿冒域名
        "subject": "Urgent: Verify Your Account",
        "links": ["http://micros0ft-login.com/verify"]
    }
)

# 用户点击并输入凭证
SecurityLog(
    log_type="web",
    source_ip="192.168.1.50",
    action="submit",
    raw_data={
        "url": "http://micros0ft-login.com/verify",
        "credential_entered": True
    }
)
```

---

## 📊 检测规则对比

| 检测类型 | 复杂度 | 误报率 | 适用场景 | 数据源 |
|---------|--------|--------|----------|--------|
| 失陷主机 | 高 | 中 | APT、僵尸网络 | 防火墙、IDS、EDR |
| 异常登录 | 中 | 低 | 账号安全 | 认证日志 |
| 数据外泄 | 高 | 中 | 数据保护 | 防火墙、代理 |
| 恶意软件 | 中 | 低 | 病毒防护 | EDR、防病毒 |
| 内部威胁 | 高 | 高 | 员工监控 | 文件访问、认证 |
| DDoS攻击 | 低 | 低 | 服务保护 | 防火墙、流量 |
| 横向移动 | 高 | 中 | APT追踪 | 防火墙、认证 |
| 钓鱼攻击 | 中 | 中 | 邮件安全 | 邮件、Web |

## 🚀 使用方法

### 单个检测

```python
from security_analysis.architecture import SecurityAnalysisSystem, AnalysisType

system = SecurityAnalysisSystem()

result = await system.analyze(
    AnalysisType.DATA_EXFILTRATION.value,
    logs
)

print(f"置信度: {result.confidence}")
print(f"发现: {result.findings}")
```

### 批量检测

```python
# 同时运行多种检测
analysis_types = [
    AnalysisType.COMPROMISED_HOST.value,
    AnalysisType.MALWARE_DETECTION.value,
    AnalysisType.DATA_EXFILTRATION.value,
]

results = await system.batch_analyze(analysis_types, logs)

for analysis_type, result in results.items():
    print(f"{analysis_type}: {result.confidence}")
```

### 完整示例

```bash
python examples/complete_security_analysis.py
```

## 🔧 自定义检测规则

### 添加新的检测器

```python
class CustomAnalyzer(BaseAgent):
    def __init__(self, framework):
        config = AgentConfig(
            name="custom_analyzer",
            description="自定义检测"
        )
        super().__init__(config)
        self.framework = framework

    async def execute(self, state: AgentState) -> AgentState:
        # 实现检测逻辑
        logs = state.get("logs", [])

        # 创建分析链
        chain = self.framework.create_chain([
            "分析日志: {input}",
            "提取特征: {input}",
            "生成报告: {input}"
        ])

        # 执行分析
        result = await chain.execute(state)

        state["analysis_result"] = {
            "type": "custom_detection",
            "report": result.get("output", "")
        }

        return state
```

### 注册到路由系统

```python
# 在 SecurityAnalysisRouter.__init__ 中添加
self.analyzers["custom_detection"] = CustomAnalyzer(framework)
```

## 📈 性能优化

### 1. 日志预处理（已实现）

系统已实现完整的两层预处理管道，替代了原有的硬编码截断方式：

**数据流**: `SecurityLog[] → PreFilter(规则筛选) → Preprocessor(统计摘要+采样) → prompt chain`

**LogPreFilter** (`security_analysis/prefilter.py`):
- 12种过滤原因（FilterReason枚举）
- 通用规则：告警动作、非工作时间、高频源IP、已知恶意端口
- 8种分析器专属规则集
- 风险评分计算

**LogPreprocessor** (`security_analysis/preprocessor.py`):
- 通用统计：IP频次、时间分布、协议/动作/端口分布、连接对
- 8种分析器专属统计方法
- 优先采样策略（告警/异常优先，补充均匀采样，默认20条）

**配置** (`config/default.yaml` → `preprocessing`段):
- `prefilter_enabled` / `preprocessor_enabled` 开关
- `sample_size`: 采样数量（默认20）
- `top_n`: Top统计数量（默认15）
- `work_hours`: 工作时间范围
- `high_freq_threshold`: 高频阈值

### 2. 并行分析
```python
# 使用批量分析并行执行
results = await system.batch_analyze(analysis_types, logs)
```

### 3. 模型选择
```python
# 为不同检测器配置不同模型
framework.set_agent_model("compromised_host_analyzer", "gpt-4o")
framework.set_agent_model("simple_classifier", "glm-4-flash")
```

## 🎓 最佳实践

1. **选择合适的检测类型**: 根据日志类型选择对应的检测器
2. **设置合理的阈值**: 根据业务特点调整检测阈值
3. **定期更新规则**: 根据新威胁更新检测逻辑
4. **结合多种检测**: 使用批量分析提高检测覆盖率
5. **验证检测结果**: 使用工具验证IOC和特征

## 📝 总结

安全分析系统现已支持**8种完整的检测规则**，覆盖：
- ✅ 主机安全（失陷主机、恶意软件）
- ✅ 账号安全（异常登录、内部威胁）
- ✅ 网络安全（DDoS、横向移动）
- ✅ 数据安全（数据外泄）
- ✅ 应用安全（钓鱼攻击）

每种检测都包含：
- 完整的分析流程
- 工具验证机制
- 详细的执行轨迹
- 可定制的报告生成